#!/usr/bin/env ruby
lingen_dir = File.expand_path(File.join(File.dirname(__FILE__), '..', 'lib'))
$LOAD_PATH.unshift(lingen_dir) unless $LOAD_PATH.include?(lingen_dir)
require 'lingen'

exit Lingen::Application.run!(*ARGV)

